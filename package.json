{
  "name": "selenium-ts-automation",
  "version": "0.1.0",
  "private": true,
  "description": "Selenium automation scaffold in TypeScript with fresh Chrome instance per run",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@types/selenium-webdriver": "^4.35.3",
    "selenium-webdriver": "^4.38.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
